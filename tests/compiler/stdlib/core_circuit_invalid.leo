// namespace: Compile
// expectation: Fail
// commented out till we have a way to test this again.

import std.unstable.blake2s.BadCircuit; // `BadCircuit` is not included in the blake2s package

function main() {}
